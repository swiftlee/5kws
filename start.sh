#!/bin/sh
# TODO: copy built jar to mods folder if not exist
cd client_start && python -m pip install -r requirements.txt
# TODO: run python script
# TODO: kill .sh process once all clients are in-game